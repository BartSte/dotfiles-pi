# MEMORY.md

- For authentication, you can use `rbw` to retrieve tokens (e.g., `rbw get github_token`).
- The bare repos `~/dotfiles.git`, `~/dotfiles-linux.git`, and `~/dotfiles-pi.git` contain the user's dotfiles.
- Detailed information about these repos is in `~/README.md`.
